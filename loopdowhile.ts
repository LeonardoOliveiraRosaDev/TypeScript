// implementar o loop do...while
let novoNum: number = 15
console.log('Valores encontrados durante a iteração do loop')

// Estabelecer o loop

do{
    console.log(novoNum)
    novoNum--
}while(novoNum <= 0) // false  , pelo menos 1 vez ele e executado